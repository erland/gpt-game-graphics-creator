# GPT Preview regression pack

Run G01–G16 after uploading the current Builder configuration and the Knowledge files listed in `builder/UPLOAD-MANIFEST.md`. Record actual result, evidence and pass/fail for each case.

G16 specifically verifies the prototype-to-production visual upgrade workflow.

These files are active regression tests, not historical preflight results. Historical executions belong in Git history, issues or release notes rather than committed result snapshots.
