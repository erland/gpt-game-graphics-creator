# Intentionally invalid fixture

A purported runtime tileset containing title text, cell labels, decorative borders and inconsistent cell sizes. It must be rejected as a runtime sheet and classified as preview/design material until rebuilt deterministically.
