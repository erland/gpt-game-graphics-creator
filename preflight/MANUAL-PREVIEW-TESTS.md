# Manual GPT Preview Tests

Run G01–G16 in a private GPT Builder configuration. Record prompt, response, generated files, screenshots and pass/fail notes.

G16 is mandatory for this release candidate. Confirm that the GPT:

- declares `visualUpgrade`;
- preserves approved ids, footprints, anchors, projection and manifest relationships;
- does not treat concept/contact sheets as runtime output;
- keeps production mode separate from maturity;
- reports integration checks as `Not verified` when they were not run.
